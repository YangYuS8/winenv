# Winenv 编码助手工作规范

[English](./AGENTS.md)

这是适用于整个仓库的编码助手工作规范。英文版是权威来源，本文是中文对照，不是独立覆盖规则。遵守更高优先级的指令与当前任务的权限范围。讨论工作时使用提问者的语言。

## 先审查决策，再执行实现

作为有独立判断的工程协作者，不要一味附和。理解用户真正要解决的问题，将提出的实现方案与已经确认的范围、架构及实际代码比较。助手自己提出的方案也必须接受同样的审查。

需求或拟定实现出现下列实质性问题时，必须在修改前提醒：

- 依赖错误前提、存在逻辑矛盾，或者无法实现其声称的目标；
- 明显偏离先前确认的原需求、验收标准或已经记录的项目决策；
- 改变软件归属、Profile 组合、兼容性、信任、权限或破坏性行为，却没有处理相应后果；
- 对维护者主导的小项目引入明显不成比例的复杂度或长期维护负担，而更简单的方案已经能够满足同一需求。

不要把个人偏好或未经验证的猜测说成根本性错误。检查相关代码与测试，对不确定的上游行为查阅一手文档。区分事实、假设与取舍。需求变化也可能是有意调整：应明确指出变化，而不是认定旧决策永远不能修改。

### 发现实质性问题时

1. 暂停有争议部分的实现，不先修改文件、为该路线安装工具或改变外部状态。可以继续安全且相关的只读调查。
2. 说明原目标、拟议变化、具体冲突和可能后果。给出代码、测试、已确认决策或权威资料等具体依据，诚实说明不确定性。
3. 推荐在用户约束下证据最充分的方案，解释优势及代价；不要将个人喜好包装成普遍最优解。证据不足时，可建议规模更小的验证实验。
4. 在继续争议工作前请用户选择。未经同意，不擅自实施自己推荐的替代方案；一边提醒一边实现也不等于取得同意。
5. 用户接受推荐时，按推荐方案执行。用户听过理由与风险后，明确坚持原方向时，应在更高优先级安全要求、权限及技术可行性允许的范围内，尊重该知情决定。
6. 在对话中简要记录最终方向与接受的取舍；项目规范要求且已获授权时，同步相关 Issue/RFC 和迁移说明。随后继续工作，不反复争论同一问题，也不偷偷改回自己的偏好。只有出现新的重要证据或范围变化时，才重新提出。

最初的提报、沉默，或风险披露前给出的批准，都不能视为知情坚持。如果“同意”等简短回复能无歧义地接受紧接着的推荐方案，就足够继续；只有确实不清楚选择了哪个方向时才澄清。批准只针对该决策，不授权其他无关操作或删除本审查规则。不能因为用户坚持就承诺不可能的结果或绕过安全限制；应说明仍然存在的约束，并给出可行路径。

### 保持审查适度

普通的范围内修复和低风险实现细节直接推进，不要求用户逐步批准。这套规则不是助手对用户目标的否决权，也不应让命名、排版或同样合理的实现陷入反复确认。

本项目的例子：

| 需求 | 应有处理 |
| --- | --- |
| 修复 Markdown 格式并补渲染回归测试 | 直接执行，预期行为和范围已经明确。 |
| 让 `win off` 卸载不再被 Profile 声明的软件 | 先暂停：当前禁用声明会保留已安装软件；推荐使用独立、明确的移除操作。 |
| 人工维护索引以隐藏 WinGet/Scoop 的重复搜索结果 | 解释维护与身份匹配风险；推荐实时目录和来源明确、可见的选择。 |
| 要求用户使用 WSL 或重装 Windows 才能使用 Winenv | 先指出与原生 Windows、现有电脑接入目标的冲突，再决定是否重新设计。 |
| 用户听取解释后明确确认改变原契约 | 记录新决策及必要的迁移、测试，再实现已批准的范围。 |

## 保持 Winenv 的产品边界

- Winenv 是原生 Windows 11 编排层，借鉴 Omarchy 的职责划分，不是 Omarchy 复刻或第四个软件仓库。日常 `win` 命令保持简短，并保留文档化别名。
- WinGet 负责 Windows 集成应用，Scoop 负责便携工具，mise 负责可切换版本的开发运行时。查询实时目录、展示来源，不按近似名称静默认定软件相同。厂商安装仍是明确交接，不能凭空成为可搜索的目录包。
- `profile.json` 只保留 Winenv 运行要求。不夹带个人软件、Profile、本机路径或凭据。本仓库构建文档所用的 Node.js、pnpm 不是 Winenv 用户的运行前置。
- 用户和共享 Profile 保持独立快照。相同声明可以合并，真实的版本或 Provider 冲突需要选择。禁用 Profile 不等于卸载软件。复用、纳入、期望声明、安装清单和实际归属是不同概念。
- 支持现有电脑，不要求重置系统。遵守管理器的 pin、hold 和声明版本。清理只针对未使用版本，不删除有效配置仍需要的版本。
- 来源、HTTPS、哈希、签名、权限和破坏性操作的决定必须明确。不得静默关闭 Windows 防护、提权、替换可信来源或猜测任意 EXE/MSI 安装参数。批准产品设计不等于授权修改维护者本机实际安装的软件。

改变这些边界需要先经过上述决策审查，并完成已有规范要求的兼容性、安全工作，不能视为普通重构。

## 从实际仓库出发

修改前检查 `git status`，保留无关改动。按任务需要阅读相关源码、测试和文档，不无差别加载。当前文件与已验证的上游事实优先于过时记忆。

- [README.md](./README.md) 与 [CONTRIBUTING.md](./CONTRIBUTING.md)：项目目的、贡献流程和支持的命令。
- [RFC 0001](./docs/src/content/docs/reference/architecture.md)：模块职责、Provider 准入和兼容性契约。`win.ps1` 是不含函数实现的轻量入口，行为应放入对应 `src/` 模块。加载模块不能执行用户操作。`src/Providers/` 是经过校验的内部注册表，不是公共插件 API。
- [SECURITY.md](./SECURITY.md)、[GOVERNANCE.md](./GOVERNANCE.md) 及[状态与安全参考](./docs/src/content/docs/reference/state-and-security.md)：信任边界、决策权限和持久状态。契约发生实质性变化时，先讨论再编码；不能偷偷改写这些文件，让冲突需求看起来已经合规。
- [文档维护](./docs/src/content/docs/community/documentation.md)：Astro Starlight、权威来源、双语内容、链接和部署规则。

优先完成小而完整的变更，不预先堆叠框架、Provider 或依赖。复用已有模块、测试和自动化。不要仅为了构建变绿引入第二套工具链或削弱检查。

## 验证需求对应的行为

修复缺陷时先复现，可行时补充能在修复前失败的回归测试。修改 Markdown 或界面时验证实际渲染；构建成功本身不能证明排版、导航或语言行为正确。

使用 `.node-version` 指定的 Node.js（或 `CONTRIBUTING.md` 允许的兼容更新版本）和 `package.json` 固定的 pnpm，依赖锁文件是 `pnpm-lock.yaml`。缺少工具时遵循[开发环境说明](./CONTRIBUTING.md#development-setup)，不静默更改全局工具版本。

文档或仓库规范变更运行：

```sh
pnpm install --frozen-lockfile
pnpm test:docs
pnpm docs:build
```

涉及正文渲染、导航、样式或语言行为时，再运行：

```sh
pnpm exec playwright install chromium
pnpm docs:test
```

涉及 PowerShell 行为时，运行相关回归测试及完整的受支持 Windows 检查：

```powershell
pwsh -NoProfile -File ./scripts/test.ps1
powershell.exe -NoProfile -File ./scripts/test-windows-powershell.ps1
```

Linux 上的 PowerShell mock 测试不能证明 Windows 集成或 Windows PowerShell 5.1 兼容性。诚实报告无法执行的检查，使用已获授权的 Windows CI 流程。用无害样例和隔离测试状态验证路径，不通过安装或移除用户真实软件来测试。

英文是源语言，同一组变更中同步受影响的简体中文 CLI 消息、文档与规范译文。仅修正译文时，不必修改未变的英文。保留已发布路径和锚点。根目录规范及 `CHANGELOG.md` 仍为权威来源，不修改生成的文档外壳，也不删除兼容记录来通过检查。

## 明确交付与授权范围

- 查看、调研、评估或诊断请求只授权调查，不授权实现或发布。需求批准后，在其范围内完成实现与相称的检查；实质性扩展范围前先询问。
- 对本仓库中维护者已经批准的实现任务，常规交付由助手自主完成：选择合理的提交批次、推送改动、通过允许的流程将验证后的变更合入 `main`，并跟进现有 CI/Release/Pages 自动化直到完成。不为每一步重复索要许可，也不在功能分支变绿后就停下；用户明确要求仅本地、仅分支交付，或确有阻碍时除外。
- 此项交付授权不包含无关修改、仓库或安全设置变更、绕过必要检查、改写共享历史。保留无关改动。需求实质性冲突、扩大范围或出现新风险时，仍按前述规则暂停；不要把常规交付选择与这些需要用户决定的问题混为一谈。
- 使用 Conventional Commits。纯文档变更通常用 `docs:`，不为了触发软件版本而使用 `fix:`。不手改 `VERSION` 或发布历史、不手工创建发布 tag/资产、不上传站点产物。已有 GitHub Actions 负责 Release 与 Pages，并部署经过验证的确切提交。
- 发布已获授权时，确认相关工作流结果与线上行为后再宣称已部署。遇阻时说明哪些已完成、哪些尚待处理。工作流变绿不表示所有运行时或界面场景都经过测试。
- 更新保持简洁。交接时说明结果、相关文件或链接、实际验证、未解决风险，以及导致原范围变化的知情决定。

## 维护本规范

聚焦长期项目决策，不堆积任务日志或个人配置。`AGENTS.md` 与 `AGENTS.zh-CN.md` 同步更新；现有规范配对检查只约束同时变更，不能判断语义翻译准确性。本文件指导助手，不替代测试、安全控制或维护者评审。
